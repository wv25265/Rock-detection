#!/bin/bash
# Double-click this file to launch the Rock Size Measurement App.
# The browser opens automatically; press Ctrl+C in this terminal window to stop.

cd "$(dirname "$0")"
echo "Starting Rock Size Measurement App..."
echo "The browser will open automatically; press Ctrl+C here to stop."
echo "-----------------------------------------------"
python3 app.py
